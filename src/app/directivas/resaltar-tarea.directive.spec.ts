import { ResaltarTareaDirective } from './resaltar-tarea.directive';

describe('ResaltarTareaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarTareaDirective();
    expect(directive).toBeTruthy();
  });
});
