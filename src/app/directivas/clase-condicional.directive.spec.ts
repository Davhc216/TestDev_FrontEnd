import { ClaseCondicionalDirective } from './clase-condicional.directive';

describe('ClaseCondicionalDirective', () => {
  it('should create an instance', () => {
    const directive = new ClaseCondicionalDirective();
    expect(directive).toBeTruthy();
  });
});
